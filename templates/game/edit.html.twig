{% extends 'base.html.twig' %}

{% block title %}Modifier le Match{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2>Modifier le Match : {{ game.equipeA.nom }} 🆚 {{ game.equipeB.nom }}</h2>

        {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.scoreEquipeA) }}
                {{ form_widget(form.scoreEquipeA, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.scoreEquipeB) }}
                {{ form_widget(form.scoreEquipeB, {'attr': {'class': 'form-control'}}) }}
            </div>

            <button type="submit" class="btn btn-success">💾 Enregistrer</button>
        {{ form_end(form) }}

        <a href="{{ path('tournoi_show', { id: game.tournoi.id }) }}" class="btn btn-secondary mt-3">Retour</a>
    </div>
{% endblock %}
